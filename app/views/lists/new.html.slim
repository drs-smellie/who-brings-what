h2 Create a list for your WhoBringsWhat event

#form-styling
  = form_for @list do |f|
    .large-3.columns
      = f.label :name, "List name"
    .large-9.columns
      = f.text_field :name, autofocus: true, placeholder: "e.g. Let's get this show on the road"

      button.button.radius type="submit" Save